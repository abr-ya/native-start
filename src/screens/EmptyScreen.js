import React from "react";
import { Text, StyleSheet, View } from "react-native";

const EmptyScreen = () => {
  return (
    <View>
      <Text style={styles.textStyle1}>Empty Screen</Text>
    </View>
  );
  
};

const styles = StyleSheet.create({

});

export default EmptyScreen;
